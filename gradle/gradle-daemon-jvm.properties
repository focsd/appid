# Gradle 8.11.1 supports running on Java 17 (and uses it for this project).
# Android Studio resolves #GRADLE_LOCAL_JAVA_HOME from this file and can
# provision the matching JDK when it is not installed locally.
toolchainVersion=17
